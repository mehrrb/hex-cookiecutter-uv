"""
API URL configuration for {{cookiecutter.project_name}}.
"""
from django.urls import path, include

urlpatterns = [
    # Add your API endpoints here
    # path('users/', include('{{cookiecutter.project_slug}}.adapters.driving.api.user_urls')),
]
